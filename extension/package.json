{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "ai-usage",
  "title": "AI Usage",
  "description": "Gemini, Claude and Codex tokens, costs, limits and local snapshots",
  "icon": "extension-icon.png",
  "author": "viggo",
  "platforms": ["macOS"],
  "categories": ["Developer Tools", "Productivity"],
  "license": "MIT",
  "commands": [
    {
      "name": "ai-usage",
      "title": "AI Usage",
      "description": "Show AI tokens, estimated costs and remaining limits",
      "mode": "view",
      "keywords": ["claude", "codex", "gemini", "tokens", "costs"]
    }
  ],
  "preferences": [
    {
      "name": "backendPath",
      "type": "textfield",
      "required": false,
      "title": "Backend Path",
      "description": "Optional absolute path to the ai-usage executable"
    }
  ],
  "scripts": {
    "build": "ray build -e dist -o dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run lint && npm run build",
    "publish": "npx @raycast/api@latest publish"
  },
  "dependencies": {
    "@raycast/api": "2.4.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "2.2.0",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "^10.10.0",
    "prettier": "^3.9.6",
    "typescript": "6.0.3"
  }
}
